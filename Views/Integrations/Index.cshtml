@{
    var project = (ViewBag.Project as HealthyCron.Models.Project)!;
    var user = (ViewBag.User as HealthyCron.Models.User)!;
    var integrations = ViewBag.Integrations as List<HealthyCron.Models.ViewModels.IntegrationListItemViewModel>;
    ViewData["Title"] = $"{(project != null ? project.Name : "Project")} - Integrations";
    Layout = "_DashboardLayout";
}

<div class="max-w-4xl mx-auto pb-20">
    <!-- Breadcrumbs -->
    <div class="flex items-center gap-2 text-text-secondary text-sm mb-6 tracking-wider font-semibold">
        <a href="/dashboard" class="hover:text-blue-500 transition-colors flex items-center gap-1">
            <i data-lucide="layout-grid" class="w-3.5 h-3.5"></i>
            Projects
        </a>
        <i data-lucide="chevron-right" class="w-3.5 h-3.5"></i>
        <a href="/project/@project.Slug/monitors" class="hover:text-white transition-colors">@project.Name</a>
        <i data-lucide="chevron-right" class="w-3.5 h-3.5"></i>
        <span class="text-white">Integrations</span>
    </div>

    <div class="flex items-center justify-between mb-8">
        <div>
            <h1 class="text-3xl font-bold text-white flex items-center gap-3">
                <i data-lucide="zap" class="text-blue"></i>
                Integrations
            </h1>
            <p class="text-text-secondary mt-1">Manage where you receive notifications for this project.</p>
        </div>
    </div>

    @{
        var successParam = Context.Request.Query["success"].FirstOrDefault();
        var errorParam = Context.Request.Query["error"].FirstOrDefault();
    }
    @if (successParam == "pushover")
    {
        <div class="mb-6 flex items-center gap-3 rounded-xl bg-amber-500/10 border border-amber-500/20 px-4 py-3 text-amber-400 text-sm font-medium">
            <i data-lucide="check-circle-2" class="w-4 h-4 shrink-0"></i>
            Pushover connected successfully! You'll start receiving alerts on your device.
        </div>
    }
    @if (errorParam == "pushover_cancelled")
    {
        <div class="mb-6 flex items-center gap-3 rounded-xl bg-rose-500/10 border border-rose-500/20 px-4 py-3 text-rose-400 text-sm font-medium">
            <i data-lucide="x-circle" class="w-4 h-4 shrink-0"></i>
            Pushover subscription was cancelled. You can try again anytime.
        </div>
    }

    <!-- 1. Connected Integrations (Top) -->
    <section class="mb-12">
        <h2 class="text-xs font-black uppercase tracking-widest text-text-secondary mb-4 flex items-center gap-2">
            <i data-lucide="link" class="w-3.5 h-3.5"></i>
            Connected Integrations
        </h2>

        @if (integrations != null && integrations.Any())
        {
            <div class="grid grid-cols-1 gap-4">
                @foreach (var item in integrations)
                {
                    var integration = item.Integration;
                    var slackDetails = item.SlackDetails;
                    var teamsDetails = item.TeamsDetails;
                    var googleChatDetails = item.GoogleChatDetails;
                    var discordDetails = item.DiscordDetails;
                    var emailDetails = item.EmailDetails;
                    var pagerDutyDetails = item.PagerDutyDetails;
                    var telegramDetails = item.TelegramDetails;
                    var pushoverDetails = item.PushoverDetails;
                    var spikeDetails = item.SpikeDetails;
                    var isSlack = integration.Type == HealthyCron.Enums.IntegrationType.Slack;
                    var isTeams = integration.Type == HealthyCron.Enums.IntegrationType.Teams;
                    var isGoogleChat = integration.Type == HealthyCron.Enums.IntegrationType.GoogleChat;
                    var isDiscord = integration.Type == HealthyCron.Enums.IntegrationType.Discord;
                    var isEmail = integration.Type == HealthyCron.Enums.IntegrationType.Email;
                    var isPagerDuty = integration.Type == HealthyCron.Enums.IntegrationType.PagerDuty;
                    var isWhatsApp = integration.Type == HealthyCron.Enums.IntegrationType.WhatsApp;
                    var isCall = integration.Type == HealthyCron.Enums.IntegrationType.Call;
                    var isTelegram = integration.Type == HealthyCron.Enums.IntegrationType.Telegram;
                    var isPushover = integration.Type == HealthyCron.Enums.IntegrationType.Pushover;
                    var isSpike = integration.Type == HealthyCron.Enums.IntegrationType.Spike;

                    <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-blue-500/20 transition-all group">
                        <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-6">
                            <div class="flex items-center gap-4">
                                @if (isSlack)
                                {
                                    <div class="w-12 h-12 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                                        <img src="/images/slack.png" class="w-7 h-7 object-contain" alt="Slack" />
                                    </div>
                                }
                                else if (isTeams)
                                {
                                    <div class="w-12 h-12 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                                        <img src="/images/teams.png" class="w-8 h-8 object-contain" alt="Microsoft Teams" />
                                    </div>
                                }
                                else if (isGoogleChat)
                                {
                                    <div class="w-12 h-12 bg-white/5 border border-white/5 rounded-xl flex items-center justify-center shrink-0">
                                        <i data-lucide="message-square" class="w-7 h-7 text-[#00897B]"></i>
                                    </div>
                                }
                                else if (isDiscord)
                                {
                                    <div class="w-12 h-12 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                                        <img src="/images/discord.png" class="w-7 h-7 object-contain" alt="Discord" />
                                    </div>
                                }
                                else if (isEmail)
                                {
                                    <div class="w-12 h-12 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                                        <img src="/images/gmail.png" class="w-7 h-7 object-contain" alt="Email" />
                                    </div>
                                }
                                else if (isPagerDuty)
                                {
                                    <div class="w-12 h-12 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                                        <img src="/images/pagerduty.png" class="w-8 h-8 object-contain" alt="PagerDuty" />
                                    </div>
                                }
                                else if (isWhatsApp)
                                {
                                    <div class="w-12 h-12 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                                        <img src="/images/whatsapp.png" class="w-8 h-8 object-contain" alt="WhatsApp" />
                                    </div>
                                }
                                else if (isCall)
                                {
                                    <div class="w-12 h-12 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                                        <img src="/images/telephone.png" class="w-7 h-7 object-contain" alt="Phone Call" />
                                    </div>
                                }
                                else if (isTelegram)
                                {
                                    <div class="w-12 h-12 bg-[#26A5E4]/10 border border-[#26A5E4]/20 rounded-xl flex items-center justify-center shrink-0">
                                        <svg class="w-7 h-7 text-[#26A5E4]" viewBox="0 0 24 24" fill="currentColor"><path d="M11.944 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0a12 12 0 0 0-.056 0zm4.962 7.224c.1-.002.321.023.465.14a.506.506 0 0 1 .171.325c.016.093.036.306.02.472-.18 1.898-.962 6.502-1.36 8.627-.168.9-.499 1.201-.82 1.23-.696.065-1.225-.46-1.9-.902-1.056-.693-1.653-1.124-2.678-1.8-1.185-.78-.417-1.21.258-1.91.177-.184 3.247-2.977 3.307-3.23.007-.032.014-.15-.056-.212s-.174-.041-.249-.024c-.106.024-1.793 1.14-5.061 3.345-.48.33-.913.49-1.302.48-.428-.008-1.252-.241-1.865-.44-.752-.245-1.349-.374-1.297-.789.027-.216.325-.437.893-.663 3.498-1.524 5.83-2.529 6.998-3.014 3.332-1.386 4.025-1.627 4.476-1.635z"/></svg>
                                    </div>
                                }
                                else if (isPushover)
                                {
                                    <div class="w-12 h-12 bg-amber-500/10 border border-amber-500/20 rounded-xl flex items-center justify-center shrink-0">
                                        <i data-lucide="bell" class="w-7 h-7 text-amber-400"></i>
                                    </div>
                                }
                                else if (isSpike)
                                {
                                    <div class="w-12 h-12 bg-amber-500/10 border border-amber-500/20 rounded-xl flex items-center justify-center shrink-0">
                                        <i data-lucide="zap" class="w-7 h-7 text-amber-400"></i>
                                    </div>
                                }
                                else
                                {
                                    <div class="w-12 h-12 bg-navy-light border border-white/5 rounded-xl flex items-center justify-center shrink-0">
                                        <i data-lucide="zap" class="w-7 h-7 text-blue-500"></i>
                                    </div>
                                }

                                <div>
                                    <h3 class="font-bold text-white text-lg">@integration.Name</h3>
                                    @if (isSlack && slackDetails != null)
                                    {
                                        <p class="text-xs text-text-secondary">Workspace: @slackDetails.WorkspaceName | Channel: #@slackDetails.ChannelName</p>
                                    }
                                    else if (isTeams && teamsDetails != null)
                                    {
                                        var maskedUrl = teamsDetails.WebhookUrl.Length > 50 
                                            ? teamsDetails.WebhookUrl.Substring(0, 50) + "..." 
                                            : teamsDetails.WebhookUrl;
                                        <p class="text-xs text-text-secondary font-mono">@maskedUrl</p>
                                    }
                                    else if (isGoogleChat && googleChatDetails != null)
                                    {
                                        if (!string.IsNullOrEmpty(googleChatDetails.SpaceName))
                                        {
                                            <p class="text-xs text-text-secondary">Space: @googleChatDetails.SpaceName</p>
                                        }
                                        else
                                        {
                                            var maskedUrl = googleChatDetails.WebhookUrl.Length > 50 
                                                ? googleChatDetails.WebhookUrl.Substring(0, 50) + "..." 
                                                : googleChatDetails.WebhookUrl;
                                            <p class="text-xs text-text-secondary font-mono">@maskedUrl</p>
                                        }
                                    }
                                    else if (isDiscord && discordDetails != null)
                                    {
                                        if (!string.IsNullOrEmpty(discordDetails.ChannelName))
                                        {
                                            <p class="text-xs text-text-secondary">Channel: @discordDetails.ChannelName</p>
                                        }
                                        else
                                        {
                                            var maskedUrl = discordDetails.WebhookUrl.Length > 50 
                                                ? discordDetails.WebhookUrl.Substring(0, 50) + "..." 
                                                : discordDetails.WebhookUrl;
                                            <p class="text-xs text-text-secondary font-mono">@maskedUrl</p>
                                        }
                                    }
                                    else if (isEmail && emailDetails != null)
                                    {
                                        <p class="text-xs text-text-secondary">@emailDetails.Email</p>
                                    }
                                    else if (isPagerDuty && pagerDutyDetails != null)
                                    {
                                        <p class="text-xs text-text-secondary">Account ID: @pagerDutyDetails.AccountId</p>
                                    }
                                    else if (isTelegram && telegramDetails != null)
                                    {
                                        var chatTypeLabel = telegramDetails.ChatType switch {
                                            "group" => "Group",
                                            "supergroup" => "Group",
                                            "channel" => "Channel",
                                            "private" => "DM",
                                            _ => "Chat"
                                        };
                                        var chatTypeBadgeColor = telegramDetails.ChatType switch {
                                            "channel" => "text-sky-400 bg-sky-400/10 border-sky-400/20",
                                            "private" => "text-violet-400 bg-violet-400/10 border-violet-400/20",
                                            _ => "text-amber-400 bg-amber-400/10 border-amber-400/20"
                                        };
                                        <div class="flex items-center gap-2 flex-wrap mt-0.5">
                                            @if (!string.IsNullOrEmpty(telegramDetails.ChatName))
                                            {
                                                <span class="text-xs text-text-secondary">@telegramDetails.ChatName</span>
                                            }
                                            <span class="inline-flex items-center px-2 py-0.5 rounded text-[10px] font-bold border @chatTypeBadgeColor">@chatTypeLabel</span>
                                        </div>
                                    }
                                    else if (isPushover && pushoverDetails != null)
                                    {
                                        var deviceLabel = string.IsNullOrEmpty(pushoverDetails.Device) ? "All devices" : pushoverDetails.Device;
                                        <p class="text-xs text-text-secondary mt-0.5">Device: @deviceLabel</p>
                                    }
                                    else if (isSpike && spikeDetails != null)
                                    {
                                        var maskedUrl = "hooks.spike.sh/****";
                                        <p class="text-xs text-text-secondary font-mono mt-0.5">@maskedUrl</p>
                                    }
                                </div>
                            </div>

                            <div class="flex flex-wrap items-center gap-2 mt-2 sm:mt-0">
                                <button onclick="openMonitorSelector('@integration.Id', '@integration.Name')" 
                                        class="flex-1 sm:flex-none flex items-center justify-center gap-2 px-3 sm:px-4 py-2 rounded-xl bg-blue/10 text-blue border border-blue-500/20 hover:bg-blue hover:text-white transition-all font-bold text-xs sm:text-sm">
                                    <i data-lucide="list-checks" class="w-3.5 h-3.5 sm:w-4 h-4"></i>
                                    Checks
                                </button>
                                <button onclick="testIntegration('@integration.Id')" 
                                        @(ViewBag.CanManage == false ? "disabled" : "")
                                        class="flex-1 sm:flex-none flex items-center justify-center gap-2 px-3 sm:px-4 py-2 rounded-xl bg-white/5 text-text-secondary border border-white/10 hover:border-blue/20 hover:text-blue transition-all font-bold text-xs sm:text-sm @(ViewBag.CanManage == false ? "opacity-50 cursor-not-allowed" : "")">
                                    <i data-lucide="play" class="w-3.5 h-3.5 sm:w-4 h-4"></i>
                                    Test
                                </button>
                                @if (ViewBag.CanManage != false)
                                {
                                    <button onclick="deleteIntegration('@integration.Id')" 
                                            class="flex-1 sm:flex-none flex items-center justify-center px-3 sm:px-4 py-2 rounded-xl bg-rose-500/10 text-rose-500 border border-rose-500/20 hover:bg-rose-500 hover:text-white transition-all font-bold text-xs sm:text-sm">
                                        <i data-lucide="trash-2" class="w-3.5 h-3.5 sm:w-4 h-4"></i>
                                        <span class="sm:inline">Delete</span>
                                    </button>
                                }
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <div class="glass-card rounded-2xl p-12 text-center border-dashed border-2 border-white/5">
                <div class="w-16 h-16 bg-white/5 rounded-2xl flex items-center justify-center mx-auto mb-4">
                    <i data-lucide="inbox" class="w-8 h-8 text-text-secondary"></i>
                </div>
                <p class="text-text-secondary font-medium">No active integrations connected yet.</p>
                <p class="text-[10px] text-text-secondary mt-1 uppercase tracking-widest font-bold">Select a service below to get started</p>
            </div>
        }
    </section>

    <!-- 2. Available Integrations (Bottom) -->
    <section>
        <h2 class="text-xs font-black uppercase tracking-widest text-text-secondary mb-4 flex items-center gap-2">
            <i data-lucide="plus-circle" class="w-3.5 h-3.5"></i>
            Available Integrations
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <!-- Slack -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-purple-500/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                        <img src="/images/slack.png" class="w-5 h-5 object-contain" alt="Slack" />
                    </div>
                    <div>
                        <h3 class="font-bold text-white">Slack</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">Modern team chat</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/slack/authorize" 
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-blue/10 group-hover:border-blue/20 group-hover:text-blue transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>

            <!-- MS Teams -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-indigo-500/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                        <img src="/images/teams.png" class="w-6 h-6 object-contain" alt="Teams" />
                    </div>
                    <div>
                        <h3 class="font-bold text-white">Teams</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">Enterprise collaboration</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/teams/add" 
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-blue/10 group-hover:border-blue/20 group-hover:text-blue transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>

            <!-- Google Chat -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-amber-500/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-white/5 border border-white/5 rounded-xl flex items-center justify-center shrink-0">
                        <i data-lucide="message-square" class="w-6 h-6 text-[#00897B]"></i>
                    </div>
                    <div>
                        <h3 class="font-bold text-white">Google Chat</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">Workspace alerts</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/googlechat/add" 
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-blue/10 group-hover:border-blue/20 group-hover:text-blue transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>

            <!-- Discord -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-indigo-500/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                        <img src="/images/discord.png" class="w-6 h-6 object-contain" alt="Discord" />
                    </div>
                    <div>
                        <h3 class="font-bold text-white">Discord</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">Community & Dev chat</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/discord/add" 
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-blue/10 group-hover:border-blue/20 group-hover:text-blue transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>

            <!-- Email -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-blue-500/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                        <img src="/images/gmail.png" class="w-6 h-6 object-contain" alt="Email" />
                    </div>
                    <div>
                        <h3 class="font-bold text-white">Email</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">Direct notifications</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/email/add" 
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-blue/10 group-hover:border-blue/20 group-hover:text-blue transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>

            <!-- PagerDuty -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-amber-500/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-white/5 border border-white/10 rounded-xl flex items-center justify-center shrink-0">
                        <img src="/images/pagerduty.png" class="w-6 h-6 object-contain" alt="PagerDuty" />
                    </div>
                    <div>
                        <h3 class="font-bold text-white">PagerDuty</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">Incident Management</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/pagerduty/authorize" 
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-blue/10 group-hover:border-blue/20 group-hover:text-blue transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>

            <!-- Webhook -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-indigo-500/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-indigo-500/10 border border-indigo-500/20 rounded-xl flex items-center justify-center shrink-0">
                        <i data-lucide="webhook" class="w-6 h-6 text-indigo-400"></i>
                    </div>
                    <div>
                        <h3 class="font-bold text-white">Webhook</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">Custom endpoints</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/webhook/add" 
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-blue/10 group-hover:border-blue/20 group-hover:text-blue transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>



            <!-- Telegram -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-[#26A5E4]/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-[#26A5E4]/10 border border-[#26A5E4]/20 rounded-xl flex items-center justify-center shrink-0">
                        <svg class="w-6 h-6 text-[#26A5E4]" viewBox="0 0 24 24" fill="currentColor"><path d="M11.944 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0a12 12 0 0 0-.056 0zm4.962 7.224c.1-.002.321.023.465.14a.506.506 0 0 1 .171.325c.016.093.036.306.02.472-.18 1.898-.962 6.502-1.36 8.627-.168.9-.499 1.201-.82 1.23-.696.065-1.225-.46-1.9-.902-1.056-.693-1.653-1.124-2.678-1.8-1.185-.78-.417-1.21.258-1.91.177-.184 3.247-2.977 3.307-3.23.007-.032.014-.15-.056-.212s-.174-.041-.249-.024c-.106.024-1.793 1.14-5.061 3.345-.48.33-.913.49-1.302.48-.428-.008-1.252-.241-1.865-.44-.752-.245-1.349-.374-1.297-.789.027-.216.325-.437.893-.663 3.498-1.524 5.83-2.529 6.998-3.014 3.332-1.386 4.025-1.627 4.476-1.635z"/></svg>
                    </div>
                    <div>
                        <h3 class="font-bold text-white">Telegram</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">Chat &amp; channel alerts</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/telegram/add"
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-[#26A5E4]/10 group-hover:border-[#26A5E4]/20 group-hover:text-[#26A5E4] transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>

            <!-- Pushover -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-orange-500/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-orange-500/10 border border-orange-500/20 rounded-xl flex items-center justify-center shrink-0">
                        <i data-lucide="bell" class="w-6 h-6 text-orange-400"></i>
                    </div>
                    <div>
                        <h3 class="font-bold text-white">Pushover</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">Push notifications</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/pushover/add"
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-orange-500/10 group-hover:border-orange-500/20 group-hover:text-orange-400 transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>

            <!-- Spike.sh -->
            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-amber-500/20 transition-all flex items-center justify-between group">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-amber-500/10 border border-amber-500/20 rounded-xl flex items-center justify-center shrink-0">
                        <i data-lucide="zap" class="w-6 h-6 text-amber-400"></i>
                    </div>
                    <div>
                        <h3 class="font-bold text-white">Spike.sh</h3>
                        <p class="text-[10px] text-text-secondary uppercase tracking-widest font-black">On-call &amp; escalations</p>
                    </div>
                </div>
                <a href="/project/@project.Slug/integrations/spike/add"
                   class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center text-text-secondary group-hover:bg-amber-500/10 group-hover:border-amber-500/20 group-hover:text-amber-400 transition-all">
                    <i data-lucide="arrow-right" class="w-5 h-5"></i>
                </a>
            </div>
        </div>
    </section>
</div>

<!-- Monitor Selector Modal -->
<div id="monitorSelectorModal" class="fixed inset-0 z-50 hidden overflow-y-auto">
    <div class="flex items-center justify-center min-h-screen p-4">
        <!-- Overlay -->
        <div class="fixed inset-0 bg-black/80 transition-opacity" onclick="closeMonitorSelector()"></div>

        <!-- Modal Panel -->
        <div class="inline-block bg-navy-light rounded-2xl text-left overflow-hidden shadow-2xl transform transition-all sm:max-w-xl w-full border border-border-color relative z-10">
            <div class="px-6 py-4 border-b border-border-color flex justify-between items-center bg-navy/50">
                <h3 class="text-xl font-bold text-white flex items-center gap-2">
                    <i data-lucide="list-checks" class="text-blue-500"></i>
                    Manage Monitors
                </h3>
                <button type="button" onclick="closeMonitorSelector()" class="text-text-secondary hover:text-white transition-colors">
                    <i data-lucide="x" class="w-6 h-6"></i>
                </button>
            </div>

            <div class="p-6">
                <div class="mb-4">
                    <h4 id="integrationNameRef" class="text-white font-bold opacity-50 uppercase text-[10px] tracking-widest mb-1">Integration</h4>
                    <p class="text-sm text-text-secondary">Select which monitors should trigger notifications for this integration.</p>
                </div>

                <div id="monitorsContainer" class="max-h-[300px] overflow-y-auto space-y-2 pr-2">
                    <!-- Loaded via JS -->
                    <div class="animate-pulse flex space-y-4 flex-col">
                        <div class="h-10 bg-white/5 rounded-xl"></div>
                        <div class="h-10 bg-white/5 rounded-xl"></div>
                        <div class="h-10 bg-white/5 rounded-xl"></div>
                    </div>
                </div>
            </div>

            <div class="bg-navy/50 px-6 py-4 flex justify-end gap-3 border-t border-border-color">
                <button type="button" onclick="closeMonitorSelector()" class="px-5 py-2 rounded-xl text-text-secondary hover:text-white font-bold text-sm">
                    Cancel
                </button>
                @if (ViewBag.CanManage != false)
                {
                    <button type="button" onclick="saveMonitorSelection()" id="saveMappingBtn" class="bg-blue hover:bg-blue/80 text-white px-8 py-2 rounded-xl font-black text-sm transition-all shadow-lg shadow-blue/20">
                        Save Changes
                    </button>
                }
            </div>
        </div>
    </div>
</div>

<script>
    let currentIntegrationId = null;

    async function openMonitorSelector(integrationId, name) {
        currentIntegrationId = integrationId;
        document.getElementById('integrationNameRef').innerText = name;
        document.getElementById('monitorSelectorModal').classList.remove('hidden');
        
        const container = document.getElementById('monitorsContainer');
        container.innerHTML = '<div class="flex justify-center py-8"><div class="w-8 h-8 border-4 border-blue border-t-transparent rounded-full animate-spin"></div></div>';

        try {
            const response = await fetch(`/integrations/${integrationId}/monitors`);
            const data = await response.json();
            
            if (data.monitors && data.monitors.length > 0) {
                container.innerHTML = data.monitors.map(m => `
                    <label class="flex items-center justify-between p-4 rounded-xl bg-navy hover:bg-white/5 border border-border-color cursor-pointer transition-all">
                        <span class="text-white font-bold text-sm">${m.name}</span>
                        <input type="checkbox" name="monitorIds" value="${m.id}" ${m.isSelected ? 'checked' : ''} 
                               class="w-5 h-5 rounded border-border-color bg-navy text-blue focus:ring-blue focus:ring-offset-navy">
                    </label>
                `).join('');
            } else {
                container.innerHTML = '<p class="text-center py-8 text-text-secondary text-sm">No monitors found in this project.</p>';
            }
        } catch (error) {
            container.innerHTML = '<p class="text-center py-8 text-rose-500 text-sm">Failed to load monitors.</p>';
        }
        
        if (typeof lucide !== 'undefined') lucide.createIcons();
    }

    function closeMonitorSelector() {
        document.getElementById('monitorSelectorModal').classList.add('hidden');
    }

    async function saveMonitorSelection() {
        const btn = document.getElementById('saveMappingBtn');
        const checkboxes = document.querySelectorAll('input[name="monitorIds"]:checked');
        const monitorIds = Array.from(checkboxes).map(cb => cb.value);

        btn.disabled = true;
        btn.innerText = 'Saving...';

        try {
            const response = await fetch(`/integrations/${currentIntegrationId}/monitors`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(monitorIds)
            });

            if (response.ok) {
                closeMonitorSelector();
            } else {
                alert('Failed to save monitor mapping.');
            }
        } catch (error) {
            alert('An error occurred while saving.');
        } finally {
            btn.disabled = false;
            btn.innerText = 'Save Changes';
        }
    }

    async function testIntegration(id) {
        if (!confirm('Send a test notification to this integration?')) return;

        try {
            const response = await fetch(`/integrations/${id}/test`, {
                method: 'POST'
            });

            if (response.ok) {
                const data = await response.json();
                alert(data.message);
            } else {
                alert('Failed to send test ping.');
            }
        } catch (error) {
            console.error(error);
            alert('An error occurred while testing.');
        }
    }

    async function deleteIntegration(id) {
        if (!confirm('Are you sure you want to delete this integration?')) return;

        try {
            const response = await fetch(`/integrations/${id}/delete`, {
                method: 'POST'
            });

            if (response.ok) {
                window.location.reload();
            } else {
                alert('Failed to delete integration.');
            }
        } catch (error) {
            console.error(error);
            alert('An error occurred while deleting.');
        }
    }

    document.addEventListener('DOMContentLoaded', () => {
        if (typeof lucide !== 'undefined') lucide.createIcons();
    });
</script>
