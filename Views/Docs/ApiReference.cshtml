@{
    Layout = "_DocsLayout";
    ViewData["Title"] = "API Reference";
    ViewData["Breadcrumb"] = "Guides  /  API Reference";
    ViewData["PrevPageTitle"] = "Cron Job Best Practices";
    ViewData["PrevPageUrl"] = "/docs/best-practices";
    ViewData["NextPageTitle"] = "Cron Expression Syntax";
    ViewData["NextPageUrl"] = "/docs/cron-syntax";
}

<h1 class="text-2xl font-semibold text-white tracking-tight">API Reference</h1>
<p class="text-sm font-normal text-[#8A8A93] leading-relaxed mt-3 mb-10">
    The HealthyCron API is designed as a standard REST API. All endpoints are secured via HTTPS, and responses are formatted as JSON.
</p>

<div class="code-container bg-[#111114] border border-white/6 rounded-xl overflow-hidden mb-12">
    <div class="flex items-center justify-between px-4 py-2.5 border-b border-white/6">
        <span class="text-[10px] font-medium uppercase tracking-widest text-[#4A4A52]">BASE URL</span>
        <button onclick="copyCode(this)" class="text-[#4A4A52] hover:text-white transition-colors">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path></svg>
        </button>
    </div>
    <div class="p-5 overflow-x-auto">
        <code class="font-mono text-sm text-[#F2F2F2]">https://healthycron.io</code>
    </div>
</div>

<h2 class="text-base font-medium text-white mb-4 mt-8">Authentication</h2>
<p class="text-sm font-normal text-[#8A8A93] leading-relaxed mb-4">
    Pinging endpoints (<code>/ping/*</code>) explicitly <strong>do not</strong> require authentication so that they can be easily executed in constrained environments (e.g. bash scripts).
</p>
<p class="text-sm font-normal text-[#8A8A93] leading-relaxed mb-12">
    Management endpoints (<code>/api/*</code>) for creating or modifying monitors require an API key. Pass it in the standard Authorization header: <code>Authorization: Bearer {api_key}</code>
</p>

<h2 class="text-base font-medium text-white mb-6">Pinging Endpoints</h2>

<!-- /ping/{uuid} -->
<div class="border-t border-white/5 py-6">
    <div class="flex items-center gap-3 mb-2">
        <span class="method-badge method-post">POST</span>
        <code class="font-mono text-sm text-white">/ping/{uuid}</code>
    </div>
    <p class="text-sm font-normal text-[#8A8A93] leading-relaxed mb-4">
        Record a successful run of your cron job. Resets the grace period timer. Accepts GET or POST.
    </p>
</div>

<!-- /ping/{uuid}/start -->
<div class="border-t border-white/5 py-6">
    <div class="flex items-center gap-3 mb-2">
        <span class="method-badge method-post">POST</span>
        <code class="font-mono text-sm text-white">/ping/{uuid}/start</code>
    </div>
    <p class="text-sm font-normal text-[#8A8A93] leading-relaxed mb-4">
        Marks the cron job as executing. HealthyCron uses the time gap between this and the next success ping to calculate duration.
    </p>
</div>

<!-- /ping/{uuid}/fail -->
<div class="border-t border-white/5 py-6 mb-8">
    <div class="flex items-center gap-3 mb-2">
        <span class="method-badge method-post">POST</span>
        <code class="font-mono text-sm text-[#E03E2D]/90">/ping/{uuid}/fail</code>
    </div>
    <p class="text-sm font-normal text-[#8A8A93] leading-relaxed mb-4">
        Explicitly triggers a failure, immediately marking the monitor as Down and firing alerts to assigned integrations.
    </p>
</div>

<h2 class="text-base font-medium text-white mb-6 mt-12">Management API (Auth Required)</h2>

<!-- GET /api/monitors -->
<div class="border-t border-white/5 py-6">
    <div class="flex items-center gap-3 mb-2">
        <span class="method-badge method-get">GET</span>
        <code class="font-mono text-sm text-white">/api/monitors</code>
    </div>
    <p class="text-sm font-normal text-[#8A8A93] leading-relaxed mb-4">
        List all monitors in the project associated with the API key.
    </p>
</div>

<!-- POST /api/monitors -->
<div class="border-t border-white/5 py-6">
    <div class="flex items-center gap-3 mb-2">
        <span class="method-badge method-post">POST</span>
        <code class="font-mono text-sm text-white">/api/monitors</code>
    </div>
    <p class="text-sm font-normal text-[#8A8A93] leading-relaxed mb-4">
        Create a new monitor. Pass the name, schedule, and grace period in the JSON body.
    </p>
    <div class="code-container bg-[#111114] border border-white/6 rounded-xl overflow-hidden mt-4">
        <div class="flex items-center justify-between px-4 py-2 border-b border-white/6 bg-[#0A0A0B]">
            <span class="text-[10px] font-medium uppercase tracking-widest text-[#4A4A52]">REQUEST BODY</span>
        </div>
        <div class="p-4 overflow-x-auto">
<pre><code class="font-mono text-xs text-[#F2F2F2]">{
  <span class="text-[#60A5FA]">"name"</span>: <span class="text-[#22C55E]">"New Monitor"</span>,
  <span class="text-[#60A5FA]">"schedule"</span>: <span class="text-[#22C55E]">"0 * * * *"</span>,
  <span class="text-[#60A5FA]">"grace_period"</span>: <span class="text-amber-500">5</span>
}</code></pre>
        </div>
    </div>
</div>

<!-- PATCH /api/monitors/{id} -->
<div class="border-t border-white/5 py-6">
    <div class="flex items-center gap-3 mb-2">
        <span class="method-badge method-patch">PATCH</span>
        <code class="font-mono text-sm text-white">/api/monitors/{id}</code>
    </div>
    <p class="text-sm font-normal text-[#8A8A93] leading-relaxed mb-4">
        Update an existing monitor's configuration. You only need to send the fields you wish to change.
    </p>
</div>

<!-- DELETE /api/monitors/{id} -->
<div class="border-t border-white/5 py-6">
    <div class="flex items-center gap-3 mb-2">
        <span class="method-badge method-delete">DELETE</span>
        <code class="font-mono text-sm text-white">/api/monitors/{id}</code>
    </div>
    <p class="text-sm font-normal text-[#8A8A93] leading-relaxed mb-4">
        Permanently delete a monitor and all its associated ping history logs.
    </p>
</div>
