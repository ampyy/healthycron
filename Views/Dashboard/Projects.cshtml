@model IEnumerable<HealthyCron.Models.Project>
@{
    ViewData["Title"] = "My Projects";
    Layout = "_DashboardLayout";
}

<!-- Page Header -->
<!-- Page Header -->
<div class="mb-8 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
    <div>
        <h1 class="text-3xl font-bold text-white mb-2">My Projects</h1>
        <p class="text-text-secondary">Organize and manage your monitoring tasks.</p>
    </div>
    <button onclick="document.getElementById('createProjectModal').classList.remove('hidden')"
        class="bg-blue hover:bg-blue/80 text-white px-5 py-2.5 rounded-xl font-bold flex items-center justify-center gap-2 transition-all shadow-lg shadow-blue/20 w-full sm:w-auto">
        <i data-lucide="plus" class="w-5 h-5"></i>
        New Project
    </button>
</div>

<!-- Projects List -->
@if (!Model.Any())
{
    <!-- Empty State -->
    <div class="glass-card rounded-xl overflow-hidden text-center py-24 border-dashed border-2 border-white/5">
        <div class="w-20 h-20 rounded-2xl bg-blue/10 flex items-center justify-center mx-auto mb-6">
            <i data-lucide="folder-plus" class="w-10 h-10 text-blue"></i>
        </div>
        <h3 class="text-2xl font-bold text-white mb-2">No projects yet</h3>
        <p class="text-text-secondary mb-8 max-w-sm mx-auto">Create a project to group your monitors by application or
            environment.</p>
        <button onclick="document.getElementById('createProjectModal').classList.remove('hidden')"
            class="bg-blue hover:bg-blue/80 text-white px-8 py-3 rounded-xl font-bold transition-all">
            Create Project
        </button>
    </div>
}
else
{
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        @foreach (var project in Model)
        {
            var colorClass = project.Color ?? "blue";
            var iconName = project.Icon ?? "folder";

            <a href="/@project.Slug/monitors"
                class="glass-card rounded-2xl p-6 group border border-border-color hover:border-@colorClass/50 transition-all duration-300 relative overflow-hidden">

                <!-- Dynamic Gradient Background -->
                <div
                    class="absolute inset-0 bg-gradient-to-br from-@colorClass/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity">
                </div>

                <div class="relative z-10">
                    <div class="flex items-center justify-between mb-6">
                        <!-- Project Icon -->
                        <div
                            class="w-12 h-12 rounded-xl bg-@colorClass/10 border border-@colorClass/20 flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                            <i data-lucide="@iconName" class="w-6 h-6 text-@colorClass"></i>
                        </div>

                        <!-- Arrow Icon -->
                        <div
                            class="p-2 rounded-lg bg-navy-light/50 border border-border-color group-hover:border-@colorClass/30 group-hover:translate-x-1 transition-all">
                            <i data-lucide="arrow-right"
                                class="w-4 h-4 text-text-secondary group-hover:text-@colorClass transition-colors"></i>
                        </div>
                    </div>

                    <h3 class="text-xl font-bold text-white mb-2 group-hover:text-@colorClass transition-colors">@project.Name
                    </h3>

                    <div class="flex items-center justify-between mt-4 pt-4 border-t border-white/5">
                        <div class="flex items-center gap-4 text-sm text-text-secondary">
                            <div class="flex items-center gap-1.5">
                                <i data-lucide="activity" class="w-4 h-4"></i>
                                <span>@project.MonitorCount Monitors</span>
                            </div>
                            <div class="flex items-center gap-1.5">
                                <i data-lucide="zap" class="w-4 h-4"></i>
                                <span>@project.InteractionsCount Beats</span>
                            </div>
                        </div>
                        <span class="text-xs font-bold text-@colorClass uppercase tracking-widest">View</span>
                    </div>
                </div>
            </a>
        }
    </div>
}

<!-- Create Project Modal -->
<div id="createProjectModal" class="fixed inset-0 z-50 hidden overflow-y-auto">
    <div class="flex items-center justify-center min-h-screen p-4">
        <div class="fixed inset-0 bg-black/70 transition-opacity"
            onclick="document.getElementById('createProjectModal').classList.add('hidden')"></div>

        <div
            class="inline-block bg-navy-light rounded-2xl text-left overflow-hidden shadow-2xl transform transition-all sm:max-w-lg w-full border border-border-color relative z-10">
            <div class="px-6 py-4 border-b border-border-color flex justify-between items-center bg-navy/50">
                <h3 class="text-xl font-bold text-white">Create New Project</h3>
                <button onclick="document.getElementById('createProjectModal').classList.add('hidden')"
                    class="text-text-secondary hover:text-white transition-colors">
                    <i data-lucide="x" class="w-6 h-6"></i>
                </button>
            </div>

            <form action="/dashboard/create-project" method="POST">
                <div class="p-6 space-y-4">
                    <div>
                        <label for="name" class="block text-sm font-semibold text-white mb-2">Project Name</label>
                        <input type="text" name="name" id="name" required
                            class="w-full bg-navy border border-border-color rounded-xl px-4 py-3 text-white placeholder-text-secondary focus:outline-none focus:ring-2 focus:ring-blue/50 transition-all"
                            placeholder="e.g. My Website, API Service">
                    </div>
                    <div class="bg-blue/5 rounded-xl p-4 border border-blue/10">
                        <p class="text-xs text-blue/80 flex gap-2">
                            <i data-lucide="info" class="w-4 h-4 shrink-0"></i>
                            Projects help you organize your monitors and control access for different teams or
                            applications.
                        </p>
                    </div>
                </div>
                <div class="bg-navy/50 px-6 py-4 sm:flex sm:flex-row-reverse gap-3 border-t border-border-color">
                    <button type="submit"
                        class="bg-blue hover:bg-blue/80 text-white px-6 py-2.5 rounded-xl font-bold transition-all shadow-lg shadow-blue/20 w-full sm:w-auto">
                        Create Project
                    </button>
                    <button type="button"
                        onclick="document.getElementById('createProjectModal').classList.add('hidden')"
                        class="px-6 py-2.5 rounded-xl border border-border-color text-text-secondary hover:text-white hover:bg-white/5 transition-all w-full sm:w-auto mt-2 sm:mt-0 font-bold">
                        Cancel
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://unpkg.com/lucide@latest"></script>
    <script>
        lucide.createIcons();
    </script>
}
